# خطة الاستجابة للاختراق — Barista Drop

خطوات أول ساعة بعد اكتشاف أي اختراق أو تسريب محتمل. المسؤول التنفيذي الوحيد حالياً: فهد. لا يوجد فريق آخر يوزَّع عليه الأدوار — كل خطوة تنفَّذها بنفسك بالترتيب.

## علامات تستدعي تفعيل هذي الخطة
- نشاط غريب بلوحة تحكم Supabase (طلبات ضخمة غير مبررة، جداول تغيّرت بدون تدخلك).
- شركة شريكة تبلّغك إن رمزها (`postback_secret`) استُخدم من جهة غير معروفة.
- فاتورة غير متوقعة من Anthropic/Railway/Netlify (مؤشر على مفتاح مسروق يُستخدم من طرف ثالث).
- أي مستخدم يبلّغ بمشكلة بحسابه (دخول لحساب مو له، بيانات ظهرت لشخص ثاني).

## الساعة الأولى — بالترتيب

**1) قفل الوصول (أول 10 دقائق)**
- بدّل كل المفاتيح الحساسة فوراً من لوحات التحكم:
  - Supabase: `service_role key` + `anon key` (Project Settings → API) + Management PAT (Account → Access Tokens).
  - Netlify: أي Environment Variable حساس + Personal Access Token.
  - Railway: `ANTHROPIC_API_KEY` وأي متغير بيئة ثاني بمحرك الوصفات.
- بعد تبديل أي مفتاح، حدّثه فوراً بكل مكان يُستخدم فيه (`.env.local` بالأدمن/الموبايل، متغيرات بيئة Netlify، متغيرات بيئة Railway) — مفتاح قديم متروك بمكان منسي يفشل الهدف من التبديل.

**2) تحديد نطاق الضرر (10-25 دقيقة)**
- افحص جدول `affiliate_purchases` و`affiliate_clicks` — هل فيه صفوف غريبة بتوقيت الحادثة؟
- افحص جدول `profiles` — هل فيه حسابات جديدة أو صلاحيات (`role`) تغيّرت بدون علمك؟
- راجع Logs بـSupabase (Logs → API/Postgres) لمعرفة أي IP/مفتاح كان مصدر النشاط الغريب.

**3) تدوير رموز الشركاء المتأثرين (25-40 دقيقة)**
- لو الاختراق يتعلق بشركة شريكة معينة (تسرب `postback_secret` تبعها تحديداً)، افتح تبويبها بلوحة الأدمن واضغط زر "تجديد الرمز" — هذا يُبطل الرمز القديم فوراً ويولّد رمز جديد، بدون ما يأثر على باقي الشركاء.
- لو الشك يشمل كل الشركاء، دوّر الرموز للكل واحد واحد.

**4) الإبلاغ (40-60 دقيقة)**
- لو تسربت بيانات شخصية لمستخدمين (إيميلات، أرقام، أي بيانات تعريفية) — هذا يفعّل التزام PDPL الإماراتي: لازم تبلغ المستخدمين المتأثرين، وحسب حجم التسريب قد يلزم إبلاغ الجهة التنظيمية المختصة. لو غير متأكد من الحجم القانوني، استشر محامي قبل اتخاذ قرار "ما يستدعي إبلاغ".
- لو الشركة الشريكة نفسها بلّغتك، ردّ عليها بتأكيد إن الرمز تم تدويره ومتى.

## بعد الساعة الأولى
- وثّق الحادثة كتابياً: وقت الاكتشاف، وش صار بالضبط، أي مفاتيح/رموز تم تدويرها، أي بيانات (إن وُجدت) تأثرت، ومتى تم كل إجراء. هذا التوثيق يحميك لو احتجت لاحقاً تثبت إنك تصرفت بسرعة ومسؤولية.
- بعد استقرار الوضع، راجع كيف صار الاختراق (مفتاح تسرب بكود عام على GitHub؟ رمز شركة انلصق بمكان غير آمن؟) وسد نفس الثغرة عشان ما تتكرر.

## قبل ما يصير أي حادث — تجهيزات تقلل الضرر مسبقاً
- لا تحط أي مفتاح أو رمز بأي مكان عام (شات، واتساب، إيميل عادي، كود يُرفع لـGitHub العام).
- خزّن نسخة من هذا الملف بمكان تقدر توصله بسرعة حتى لو جهازك مو معك (مثلاً ملاحظة بالجوال).
